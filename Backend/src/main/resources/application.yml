spring:
  application:
    name: Lost_And_Found

  datasource:
    url: jdbc:mysql://localhost:3306/lost_and_found
    username: root
    password: Shiven@123
    dbcp2:
      driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
